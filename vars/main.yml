---
# vars file for elastic-elasticsearch
elasticsearch_home: /usr/share/elasticsearch
elasticsearch_data: "{{ elasticsearch_home }}/data"
elasticsearch_logs: "{{ elasticsearch_home }}/logs"
elasticsearch_config: "{{ elasticsearch_home }}/config"
